export enum ERoutes {
  home = '/',
  auth = '/auth',
  profile = '/profile',
  plans = '/plans',
  privacy = '/privacy',
  terms = '/terms',
  contact = '/contact',
  rooms = '/rooms',
  pending = '/rooms/pending',
  room = '/rooms/:id',
}
